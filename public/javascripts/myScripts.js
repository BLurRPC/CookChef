function createNewArticle(title, picture, text) {
    var obj = {};
    obj.title = title;
    obj.picture = picture;
    obj.text = text;
    return obj;
  }
  var myArticles = {};
  myFirstArticle = cre